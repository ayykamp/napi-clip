{
  "name": "@ayykamp/napi-clip",
  "version": "0.1.7",
  "description": "Node.js wrapper for the C++ Clip library providing cross plattform support to copy/paste clipboard content",
  "repository": {
    "type": "git",
    "url": "https://github.com/AyyKamp/napi-clip.git"
  },
  "main": "index.js",
  "scripts": {
    "install": "cmake-js compile",
    "build": "cmake-js compile",
    "build:dev": "cmake-js compile --debug",
    "test": "jest",
    "//": "Only run prebuild from GH actions",
    "prebuild": "./node_modules/.bin/prebuild --all --backend cmake-js --runtime napi -u "
  },
  "author": "AyyKamp",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.5.0",
    "cmake-js": "^6.0.0",
    "node-addon-api": "^1.7.1"
  },
  "binary": {
    "napi_versions": [3, 4 , 5]
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "prebuild": "^9.1.1"
  }
}
